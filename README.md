# PDF Optimizer Suite v2.1.1

Этот проект — профессиональный инструмент для оптимизации PDF, интегрируемый прямо в контекстное меню Проводника Windows. Позволяет уменьшать размер документов одним кликом.

## 🚀 Возможности
*   **3 уровня сжатия:** 150, 200 и 300 DPI.
*   **Офлайн установка:** Все необходимые компоненты (Python, ImageMagick) уже включены в комплект.
*   **Безопасность:** Создает новый файл (напр. `doc_150dpi.pdf`), не повреждая оригинал.
*   **Уведомления:** Показывает отчет о результатах сжатия.

## 📦 Комплект поставки
В папке `resources/` находятся официальные инсталляторы:
1.  **Python 3.12** (x64)
2.  **ImageMagick 7.1** (x64)

## 🛠 Установка (One-Click)
1.  Поместите папку `pdf-optimizer-win` в постоянное место (например, `C:\Program Files\PDFOptimizer` или `C:\Tools\`).
    *   *Важно: Не удаляйте папку после установки, так как меню будет ссылаться на скрипты внутри неё.*
2.  Запустите файл **`Setup_Full.bat`**.
3.  Скрипт автоматически проверит наличие компонентов:
    *   Если нет **Python**: Запустится установщик. **ОБЯЗАТЕЛЬНО** поставьте галочку **"Add Python.exe to PATH"** внизу окна.
    *   Если нет **ImageMagick**: Запустится установщик. **ОБЯЗАТЕЛЬНО** поставьте галочки:
        *   "Install legacy utilities (e.g. convert)"
        *   "Add application directory to your system path"
4.  После установки компонентов скрипт добавит пункты в контекстное меню (потребуются права администратора).

## 🖱️ Использование
1.  Нажмите **правую кнопку мыши** на любом PDF файле.
2.  В меню выберите **"Optimize PDF (150 DPI)"** (или другое качество).
3.  Дождитесь сообщения о результате.

## 🗑️ Удаление
Запустите `install/uninstall.bat` для очистки контекстного меню. Сами программы (Python, ImageMagick) удаляются стандартно через "Установку и удаление программ".

## ⚖️ Лицензирование
Этот проект распространяется под лицензией **MIT**.

**Сторонние компоненты:**
Дистрибутив включает в себя инсталляторы стороннего ПО, распространяемые согласно их лицензиям:
*   **Python:** Python Software Foundation License (PSF).
*   **ImageMagick:** ImageMagick License (Apache 2.0 style).

Подробная информация доступна в файле `THIRD_PARTY_NOTICES.md` внутри папки проекта.

---
© 2026 Dom Corp.
